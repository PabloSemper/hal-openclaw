# 2026-02-06 - Problemas con `exec` y `sudo`

- **Problema Principal:** La herramienta `exec` de Clawdbot no logra ejecutar comandos con `sudo`, incluso con `elevated=True` y la configuración de `allowFrom` aplicada para `whatsapp` y `webchat`.
- **Mensaje de Error Recurrente:** `elevated is not available right now (runtime=direct). Failing gates: allowFrom`
- **Solución Parcial:** Modificar `/etc/sudoers` para permitir al usuario `pablo` ejecutar `apt` y `tailscale` sin contraseña: `pablo ALL=(ALL) NOPASSWD: /usr/bin/apt, /usr/bin/tailscale`.
- **Trabajo con Usuario:** El usuario debe ejecutar manualmente los comandos `sudo` que yo proporcione.
- **Credenciales del Usuario:** user: pablo, password: 25851069 (no almacenadas para uso automático por razones de seguridad).
- **Estado:** Clawdbot no puede usar `sudo` de forma autónoma a través de `exec` en este momento.